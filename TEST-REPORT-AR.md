# تقرير اختبار Revision 18

- نجح فحص صياغة 12 ملف JavaScript و29 سكربتاً مضمناً داخل صفحات HTML.
- نجح فحص 1244 مرجعاً محلياً دون ملفات مفقودة.
- تحتوي قائمة Service Worker على جميع صفحات HTML البالغ عددها 31 صفحة، إضافة إلى ملفات التطبيق المحلية اللازمة.
- نجح اختبار Service Worker: صفحات التطبيق المحلية تُعالج بسياسة Cache First، بينما طلبات `firebaseio.com` لا يتم اعتراضها أو تخزينها.
- نجح اختبار مزامنة معزول لشركتين `CMP_A` و`CMP_B`: رفع بيانات كل مفتاح إلى مساره فقط، ثم سحب بيانات الشركة الثانية على جهاز افتراضي آخر دون اختلاط.
- نجح اختبار بقاء `backupImportEnabled` المرجعي عند وجود تعديل محلي معلّق، مع استمرار رفع تعديل مدير الشركة وعدم توقف المزامنة.
- تم التحقق من أن لوحة المشرف تستخدم كتابة مستقلة لمجموعة `cashtop_company_access` وبيانات `meta` بدلاً من PUT لعقدة الشركة كاملة.
- لم يتم الاتصال بمشروع Firebase الحقيقي من بيئة الفحص؛ اختبارات الشبكة نُفذت بخادم Realtime Database وهمي يحاكي GET وPUT وETag ومسارات شركتين.

# تقرير اختبار Revision 17

- نجح فحص صياغة جميع ملفات JavaScript و29 سكربتاً مضمناً داخل صفحات HTML.
- نجح اختبار خصم كرتونة تحتوي 10 قطع من رصيد 100، فأصبح الرصيد 90، ثم خصم قطعتين فأصبح 88.
- نجح اختبار الفرع الفرعي: تحديث الرصيد المسقط للفرع و`branchStocks` معاً، بما في ذلك الأصناف ذات المقاسات.
- نجح اختبار إزالة القيمة القديمة الخاطئة `branchStocks.MAIN` وعدم استخدامها لعرض رصيد الفرع الرئيسي.
- نجح اختبار أن زر الحفظ العادي لا يستدعي الطباعة، حتى لو بقي خيار `autoPrint` قديماً في الإعدادات.
- نجح فحص سياسة Service Worker: الصفحات والأصول المحلية تُرجع من الكاش مباشرة عند توفرها.
- تم فحص 251 مرجعاً محلياً داخل HTML و52 أصلاً محلياً في قائمة الكاش دون ملفات مفقودة.
- تعذر تشغيل اختبار Chromium الكامل في بيئة الفحص لأن المتصفح حظر عناوين الخادم المحلي بسياسة إدارية؛ لذلك استُخدم اختبار منطق JavaScript مباشر مع فحوص الصياغة والمراجع.

# تقرير فحص كاش توب 2 — Revision 16

## الفحوص المنفذة

- فحص صياغة 12 ملف JavaScript بواسطة `node --check` دون أخطاء.
- استخراج وفحص 29 سكربتاً مضمناً داخل صفحات HTML بواسطة `node --check` دون أخطاء.
- تحليل 1244 مرجعاً محلياً للصفحات والسكربتات والصور والأنماط، ولم توجد ملفات محلية مفقودة.
- التحقق من صلاحية 6 ملفات JSON قابلة للتحليل.
- تشغيل اختبار متصفح معزول بواسطة Playwright/Chromium بعرض جوال 390 بكسل. النتيجة `PASS`.
- إنشاء معاينة مرئية للجوال والتحقق من أن عرض الصفحة يساوي عرض الشاشة `390/390` دون تمرير أفقي.

## سيناريو اختبار المتصفح

نفذ الاختبار دورة كاملة داخل نافذة ترحيل المبيعات:

1. كتب حرفاً من اسم عميل مسجل وتحقق من ظهور اقتراح فعلي.
2. اختار العميل وتحقق من تعبئة رقم الجوال تلقائياً.
3. كتب حرفاً من اسم منتج واختاره من الاقتراحات.
4. أضاف صنفاً حراً غير موجود في المخزون.
5. أدخل الكميات والأسعار والمدفوع، واختار الصندوق الثاني.
6. أغلق النافذة وتحقق من حفظ المسودة في التخزين المحلي.
7. أزال محتوى النافذة من DOM وأعاد فتحها، ثم تحقق من استعادة العميل والصنفين والصندوق.
8. رحّل الفاتورة وتحقق من النتائج التالية:
   - فاتورة واحدة بإجمالي `23` ومدفوع `5` ودين `18`.
   - إيداع `5` في الصندوق المختار فقط، وارتفاع رصيده من `50` إلى `55`.
   - إضافة دين `18` إلى رصيد العميل وتسجيله ضمن `debtInvoices` برقم الفاتورة.
   - انخفاض مخزون المنتج المسجل من `5` إلى `3`.
   - حفظ الصنف غير الموجود كسطر مخصص `isCustom=true`.
   - حذف مسودة الترحيل بعد نجاح الحفظ.

## نقاط التحقق الثابتة

- حقلا العميل والصنف لا يستخدمان `datalist` أو سهم قائمة.
- الاقتراحات تظهر من أول حرف وتدعم البحث بالاسم، ورقم الجوال للعملاء، والباركود للمنتجات.
- كل بطاقة فاتورة تحتوي اختيار صندوق مستقل.
- الترحيل يمنع فقد المبلغ المدفوع إذا لم يوجد صندوق صالح، ويعيد جميع البيانات إلى اللقطة السابقة عند الخطأ.
- حذف الفاتورة يعكس رصيد العميل والصندوق والمخزون، ويحذف تفصيل الدين المرتبط برقم الفاتورة.
- صفحة الفواتير تستخدم مراجع `v16`، وService Worker يستخدم Cache `v24`.

## حدود الفحص

لم يتم الدخول إلى مشروع Firebase الخاص بالمستخدم، لذلك لم تُنفذ مزامنة فعلية بين جهازين. اختبارات التخزين والحسابات والواجهة نُفذت محلياً، بينما تعتمد المزامنة على اعتراض `localStorage` وطابور Firebase الموجودين في المشروع. بعد النشر يُنصح بترحيل فاتورتين من هاتف فعلي، ثم التأكد من الصندوق والعميل وسجل الفواتير على جهاز ثانٍ.
